# Maryum Akram

## Machine Learning Engineer

**Machine Learning Engineer building reproducible, evaluated, and deployable ML systems.** Experience spans text classification, suitability regression, structured extraction, model artifacts, deterministic training, API delivery, Docker, and CI validation.

## Selected engineering projects

### Resume Screener

Built a two-stage resume intelligence pipeline using TF-IDF, Logistic Regression, Gradient Boosting, and sentence-embedding features. Evaluated category classification at 68.0% accuracy and suitability regression at MAE 8.69 and R² 0.814 across documented datasets and model comparisons. Added deterministic Stage 1 and Stage 2 training scripts, seed-42 data splits, saved vectorizers and estimators, model manifests, SHA-256 run records, artifact verification, Docker, and CI smoke checks.

### Receipt Extractor API

Built and evaluated a FastAPI structured-extraction service with an 8-case hand-labelled regression suite covering clean, incomplete, ambiguous, and non-receipt inputs. Added schema validation, bounded repair, quarantine routing, retries, needs-review handling, latency/error benchmarking, Docker, and CI.

### RoshanAI

Built a live-data career intelligence pipeline combining job ingestion, structured model reasoning, skill-gap analysis, and proposal generation. Documented the boundary between product validation and unbenchmarked AI-generated estimates, with server-side provider separation and deployment automation.

### SkillSync AI

Built a secure AI product with authenticated workflows, rate limiting, audit logs, adversarial payload tests, and 94/94 CI tests. Documented where larger labelled evaluation data is still needed for recommendation quality.

## ML engineering strengths

Reproducible training, dataset contracts, feature engineering, baseline comparison, held-out evaluation, error analysis, model artifacts, model-run manifests, deterministic smoke tests, regression evaluation, scikit-learn, pandas, NumPy, FastAPI, Streamlit, Docker, GitHub Actions, and production-oriented failure handling.

## Interview focus

Lead with the data contract, baseline, split strategy, feature design, metric choice, error analysis, artifact version, serving boundary, and known limitations. Treat synthetic negative examples as heuristic labels and distinguish them from human-labelled evaluation data.
